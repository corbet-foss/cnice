[package]
name = "cnice"
version = "0.1.2"
edition = "2024"
rust-version = "1.94"
description = "Formulaic correspondence phrases: German salutations and locale-specific valedictions (cnice.greet + cnice.farewell)"
license = "LGPL-3.0-only WITH LGPL-3.0-linking-exception"
repository = "https://github.com/corbet-foss/cnice"

documentation = "https://docs.rs/cnice"
keywords = ["i18n", "correspondence", "locale"]
categories = ["internationalization", "text-processing"]

include = ["Cargo.toml", "Cargo.lock", "src/**", "tables/**", "tests/**", "README.md", "CHANGELOG.md", "LICENSE", "LICENSE.md", "LICENSES/**"]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
missing_errors_doc = "allow"
missing_panics_doc = "allow"
too_many_lines = "allow"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
